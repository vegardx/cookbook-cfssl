---
driver:
  name: vagrant

provisioner:
  name: chef_zero

platforms:
  - name: debian-9.3

suites:
  - name: default
    run_list:
      - "recipe[chef-cfssl::default]"
      - "recipe[chef-cfssl::install]"
      - "recipe[chef-cfssl::gen_ca]"
      - "recipe[chef-cfssl::server]"
      - "recipe[chef-cfssl::client]"
      - "recipe[chef-cfssl::gen_cert]"
    attributes:
