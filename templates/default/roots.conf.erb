<% node['cfssl']['ca'].each do |ca, val| %>
[ <%= ca %> ]
private = file:///etc/cfssl/ca/<%= ca %>-key.pem
certificate = /etc/cfssl/ca/<%= ca %>.pem
config = /etc/cfssl/conf/server/<%= ca %>.json
<% end %>
